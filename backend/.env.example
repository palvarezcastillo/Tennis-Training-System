SUPABASE_URL=
# Key secreta del backend (Supabase → Project Settings → API → service_role).
# NUNCA exponer en el frontend ni commitear el valor real.
SUPABASE_SERVICE_ROLE_KEY=
# Fallback solo si no se usa service_role (no recomendado con RLS estricto).
SUPABASE_ANON_KEY=
ANTHROPIC_API_KEY=
PORT=3001
